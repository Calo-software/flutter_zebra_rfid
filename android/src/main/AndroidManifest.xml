<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="nz.calo.flutter_zebra_rfid">
  
  <!--BLUETOOTH PERMISSION-->
    <!-- Request legacy Bluetooth permissions on older devices. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"  />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <!-- Needed only for communicating with already-paired Bluetooth devices. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
            android:name="android.permission.BLUETOOTH_SCAN"
            android:usesPermissionFlags="neverForLocation" />
  
  <!-- USB PERMISSION for USB-connected Zebra RFID readers -->
    <uses-permission android:name="android.permission.USB_PERMISSION" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <!-- Optional Qualcomm RFID service used by Zebra's QC_SERIAL fallback. -->
    <application>
      <uses-library android:name="com.qti.qrs.qc_sdk" android:required="false" />
    </application>
</manifest>
