// Autogenerated from Pigeon (v21.2.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")


import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer

private fun wrapResult(result: Any?): List<Any?> {
  return listOf(result)
}

private fun wrapError(exception: Throwable): List<Any?> {
  return if (exception is FlutterRfidError) {
    listOf(
      exception.code,
      exception.message,
      exception.details
    )
  } else {
    listOf(
      exception.javaClass.simpleName,
      exception.toString(),
      "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
    )
  }
}

private fun createConnectionError(channelName: String): FlutterRfidError {
  return FlutterRfidError("channel-error",  "Unable to establish connection on channel: '$channelName'.", "")}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterRfidError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : Throwable()

enum class ReaderConnectionType(val raw: Int) {
  BLUETOOTH(0),
  USB(1),
  ALL(2);

  companion object {
    fun ofRaw(raw: Int): ReaderConnectionType? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

enum class ReaderConnectionStatus(val raw: Int) {
  CONNECTING(0),
  CONNECTED(1),
  DISCONNECTING(2),
  DISCONNECTED(3),
  ERROR(4);

  companion object {
    fun ofRaw(raw: Int): ReaderConnectionStatus? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

enum class BluetoothScanStatus(val raw: Int) {
  SCANNING(0),
  FINISHED(1),
  ERROR(2);

  companion object {
    fun ofRaw(raw: Int): BluetoothScanStatus? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

enum class ReaderErrorCode(val raw: Int) {
  UNKNOWN(0),
  NO_AVAILABLE_READERS(1),
  INVALID_READER_INDEX(2),
  READER_DEVICE_NULL(3),
  ALREADY_CONNECTING(4),
  NOT_CONNECTED(5),
  SDK_INVALID_USAGE(6),
  SDK_OPERATION_FAILURE(7),
  TIMEOUT(8);

  companion object {
    fun ofRaw(raw: Int): ReaderErrorCode? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

enum class ReaderConfigBatchMode(val raw: Int) {
  AUTO(0),
  ENABLED(1),
  DISABLED(2);

  companion object {
    fun ofRaw(raw: Int): ReaderConfigBatchMode? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

enum class ReaderBeeperVolume(val raw: Int) {
  QUIET(0),
  LOW(1),
  MEDIUM(2),
  HIGH(3);

  companion object {
    fun ofRaw(raw: Int): ReaderBeeperVolume? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class ReaderError (
  val code: ReaderErrorCode,
  val message: String,
  val details: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ReaderError {
      val code = pigeonVar_list[0] as ReaderErrorCode
      val message = pigeonVar_list[1] as String
      val details = pigeonVar_list[2] as String?
      return ReaderError(code, message, details)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      code,
      message,
      details,
    )
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class Reader (
  val name: String? = null,
  val id: Long,
  val info: ReaderInfo? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): Reader {
      val name = pigeonVar_list[0] as String?
      val id = pigeonVar_list[1].let { num -> if (num is Int) num.toLong() else num as Long }
      val info = pigeonVar_list[2] as ReaderInfo?
      return Reader(name, id, info)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      name,
      id,
      info,
    )
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class BluetoothDevice (
  val name: String? = null,
  val address: String,
  val isPaired: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): BluetoothDevice {
      val name = pigeonVar_list[0] as String?
      val address = pigeonVar_list[1] as String
      val isPaired = pigeonVar_list[2] as Boolean
      return BluetoothDevice(name, address, isPaired)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      name,
      address,
      isPaired,
    )
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class ReaderConfig (
  val transmitPowerIndex: Long? = null,
  val tari: Long? = null,
  val beeperVolume: ReaderBeeperVolume? = null,
  val enableDynamicPower: Boolean? = null,
  val enableLedBlink: Boolean? = null,
  val batchMode: ReaderConfigBatchMode? = null,
  val scanBatchMode: ReaderConfigBatchMode? = null,
  val rfModeTableIndex: Long? = null,
  val receiveSensitivityIndex: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ReaderConfig {
      val transmitPowerIndex = pigeonVar_list[0].let { num -> if (num is Int) num.toLong() else num as Long? }
      val tari = pigeonVar_list[1].let { num -> if (num is Int) num.toLong() else num as Long? }
      val beeperVolume = pigeonVar_list[2] as ReaderBeeperVolume?
      val enableDynamicPower = pigeonVar_list[3] as Boolean?
      val enableLedBlink = pigeonVar_list[4] as Boolean?
      val batchMode = pigeonVar_list[5] as ReaderConfigBatchMode?
      val scanBatchMode = pigeonVar_list[6] as ReaderConfigBatchMode?
      val rfModeTableIndex = pigeonVar_list[7].let { num -> if (num is Int) num.toLong() else num as Long? }
      val receiveSensitivityIndex = pigeonVar_list[8].let { num -> if (num is Int) num.toLong() else num as Long? }
      return ReaderConfig(transmitPowerIndex, tari, beeperVolume, enableDynamicPower, enableLedBlink, batchMode, scanBatchMode, rfModeTableIndex, receiveSensitivityIndex)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      transmitPowerIndex,
      tari,
      beeperVolume,
      enableDynamicPower,
      enableLedBlink,
      batchMode,
      scanBatchMode,
      rfModeTableIndex,
      receiveSensitivityIndex,
    )
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class ReaderInfo (
  val transmitPowerLevels: List<Any?>,
  val firmwareVersion: String? = null,
  val modelVersion: String? = null,
  val scannerName: String? = null,
  val serialNumber: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ReaderInfo {
      val transmitPowerLevels = pigeonVar_list[0] as List<Any?>
      val firmwareVersion = pigeonVar_list[1] as String?
      val modelVersion = pigeonVar_list[2] as String?
      val scannerName = pigeonVar_list[3] as String?
      val serialNumber = pigeonVar_list[4] as String?
      return ReaderInfo(transmitPowerLevels, firmwareVersion, modelVersion, scannerName, serialNumber)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      transmitPowerLevels,
      firmwareVersion,
      modelVersion,
      scannerName,
      serialNumber,
    )
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class ReaderRegion (
  val code: String,
  val name: String? = null,
  val standardName: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ReaderRegion {
      val code = pigeonVar_list[0] as String
      val name = pigeonVar_list[1] as String?
      val standardName = pigeonVar_list[2] as String?
      return ReaderRegion(code, name, standardName)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      code,
      name,
      standardName,
    )
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class RfidTag (
  val id: String,
  val rssi: Long,
  val relativeDistance: Double? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): RfidTag {
      val id = pigeonVar_list[0] as String
      val rssi = pigeonVar_list[1].let { num -> if (num is Int) num.toLong() else num as Long }
      val relativeDistance = pigeonVar_list[2] as Double?
      return RfidTag(id, rssi, relativeDistance)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      id,
      rssi,
      relativeDistance,
    )
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class BatteryData (
  val level: Long,
  val isCharging: Boolean,
  val cause: String
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): BatteryData {
      val level = pigeonVar_list[0].let { num -> if (num is Int) num.toLong() else num as Long }
      val isCharging = pigeonVar_list[1] as Boolean
      val cause = pigeonVar_list[2] as String
      return BatteryData(level, isCharging, cause)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      level,
      isCharging,
      cause,
    )
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class Diagnostics (
  val connectionState: ReaderConnectionStatus,
  val connectAttempts: Long,
  val lastErrorCode: ReaderErrorCode? = null,
  val lastErrorMessage: String? = null,
  val lastConnectStartMs: Long? = null,
  val lastConnectDurationMs: Long? = null,
  val isLocating: Boolean,
  val scanningEnabled: Boolean? = null,
  val scanningEnabledLastToggleMs: Long? = null,
  val inventoryActive: Boolean? = null,
  val lastInventoryStartMs: Long? = null,
  val lastInventoryStopMs: Long? = null,
  val pendingPurgeActive: Boolean? = null,
  val lastInventoryStopReason: String? = null,
  val lastInventoryStartReason: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): Diagnostics {
      val connectionState = pigeonVar_list[0] as ReaderConnectionStatus
      val connectAttempts = pigeonVar_list[1].let { num -> if (num is Int) num.toLong() else num as Long }
      val lastErrorCode = pigeonVar_list[2] as ReaderErrorCode?
      val lastErrorMessage = pigeonVar_list[3] as String?
      val lastConnectStartMs = pigeonVar_list[4].let { num -> if (num is Int) num.toLong() else num as Long? }
      val lastConnectDurationMs = pigeonVar_list[5].let { num -> if (num is Int) num.toLong() else num as Long? }
      val isLocating = pigeonVar_list[6] as Boolean
      val scanningEnabled = pigeonVar_list[7] as Boolean?
      val scanningEnabledLastToggleMs = pigeonVar_list[8].let { num -> if (num is Int) num.toLong() else num as Long? }
      val inventoryActive = pigeonVar_list[9] as Boolean?
      val lastInventoryStartMs = pigeonVar_list[10].let { num -> if (num is Int) num.toLong() else num as Long? }
      val lastInventoryStopMs = pigeonVar_list[11].let { num -> if (num is Int) num.toLong() else num as Long? }
      val pendingPurgeActive = pigeonVar_list[12] as Boolean?
      val lastInventoryStopReason = pigeonVar_list[13] as String?
      val lastInventoryStartReason = pigeonVar_list[14] as String?
      return Diagnostics(connectionState, connectAttempts, lastErrorCode, lastErrorMessage, lastConnectStartMs, lastConnectDurationMs, isLocating, scanningEnabled, scanningEnabledLastToggleMs, inventoryActive, lastInventoryStartMs, lastInventoryStopMs, pendingPurgeActive, lastInventoryStopReason, lastInventoryStartReason)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      connectionState,
      connectAttempts,
      lastErrorCode,
      lastErrorMessage,
      lastConnectStartMs,
      lastConnectDurationMs,
      isLocating,
      scanningEnabled,
      scanningEnabledLastToggleMs,
      inventoryActive,
      lastInventoryStartMs,
      lastInventoryStopMs,
      pendingPurgeActive,
      lastInventoryStopReason,
      lastInventoryStartReason,
    )
  }
}
private object FlutterZebraRfidPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as Int?)?.let {
          ReaderConnectionType.ofRaw(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as Int?)?.let {
          ReaderConnectionStatus.ofRaw(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as Int?)?.let {
          BluetoothScanStatus.ofRaw(it)
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as Int?)?.let {
          ReaderErrorCode.ofRaw(it)
        }
      }
      133.toByte() -> {
        return (readValue(buffer) as Int?)?.let {
          ReaderConfigBatchMode.ofRaw(it)
        }
      }
      134.toByte() -> {
        return (readValue(buffer) as Int?)?.let {
          ReaderBeeperVolume.ofRaw(it)
        }
      }
      135.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ReaderError.fromList(it)
        }
      }
      136.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          Reader.fromList(it)
        }
      }
      137.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          BluetoothDevice.fromList(it)
        }
      }
      138.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ReaderConfig.fromList(it)
        }
      }
      139.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ReaderInfo.fromList(it)
        }
      }
      140.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ReaderRegion.fromList(it)
        }
      }
      141.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RfidTag.fromList(it)
        }
      }
      142.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          BatteryData.fromList(it)
        }
      }
      143.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          Diagnostics.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is ReaderConnectionType -> {
        stream.write(129)
        writeValue(stream, value.raw)
      }
      is ReaderConnectionStatus -> {
        stream.write(130)
        writeValue(stream, value.raw)
      }
      is BluetoothScanStatus -> {
        stream.write(131)
        writeValue(stream, value.raw)
      }
      is ReaderErrorCode -> {
        stream.write(132)
        writeValue(stream, value.raw)
      }
      is ReaderConfigBatchMode -> {
        stream.write(133)
        writeValue(stream, value.raw)
      }
      is ReaderBeeperVolume -> {
        stream.write(134)
        writeValue(stream, value.raw)
      }
      is ReaderError -> {
        stream.write(135)
        writeValue(stream, value.toList())
      }
      is Reader -> {
        stream.write(136)
        writeValue(stream, value.toList())
      }
      is BluetoothDevice -> {
        stream.write(137)
        writeValue(stream, value.toList())
      }
      is ReaderConfig -> {
        stream.write(138)
        writeValue(stream, value.toList())
      }
      is ReaderInfo -> {
        stream.write(139)
        writeValue(stream, value.toList())
      }
      is ReaderRegion -> {
        stream.write(140)
        writeValue(stream, value.toList())
      }
      is RfidTag -> {
        stream.write(141)
        writeValue(stream, value.toList())
      }
      is BatteryData -> {
        stream.write(142)
        writeValue(stream, value.toList())
      }
      is Diagnostics -> {
        stream.write(143)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface FlutterZebraRfid {
  /** Returns list with names of available readers for specified `connectionType`. */
  fun updateAvailableReaders(connectionType: ReaderConnectionType, callback: (Result<Unit>) -> Unit)
  /** Starts Bluetooth classic device discovery for pairing. */
  fun startBluetoothScan(callback: (Result<Unit>) -> Unit)
  /** Stops Bluetooth classic device discovery. */
  fun stopBluetoothScan(callback: (Result<Unit>) -> Unit)
  /** Returns currently bonded Bluetooth devices. */
  fun getBondedDevices(callback: (Result<List<BluetoothDevice>>) -> Unit)
  /** Starts pairing with the Bluetooth device at `address`. */
  fun pairBluetoothDevice(address: String, callback: (Result<Unit>) -> Unit)
  /** Connects to a reader with `readerId` ID. */
  fun connectReader(readerId: Long, callback: (Result<Unit>) -> Unit)
  /** Configures reader with `config`. */
  fun configureReader(config: ReaderConfig, shouldPersist: Boolean, callback: (Result<Unit>) -> Unit)
  /** Disconnects a current reader. */
  fun disconnectReader(callback: (Result<Unit>) -> Unit)
  /** Trigger device status */
  fun triggerDeviceStatus(callback: (Result<Unit>) -> Unit)
  /**
   * Start locating the specified `tags`.
   * If `disableBeep` is true, the reader will not beep for tags not in the locate list.
   */
  fun startLocating(tags: List<RfidTag>, disableBeep: Boolean?, callback: (Result<Unit>) -> Unit)
  /** Stop locating tags. */
  fun stopLocating(callback: (Result<Unit>) -> Unit)
  /** Reset the locate state (clears session, allows new locate operations). */
  fun resetLocateState(callback: (Result<Unit>) -> Unit)
  /** Reader currently in use */
  fun currentReader(): Reader?
  /** Reader config */
  fun readerConfig(callback: (Result<ReaderConfig>) -> Unit)
  /** Supported regulatory regions for the current or last-selected reader. */
  fun supportedReaderRegions(callback: (Result<List<ReaderRegion>>) -> Unit)
  /** Applies a regulatory region to the current reader. */
  fun setReaderRegion(regionCode: String, callback: (Result<Unit>) -> Unit)
  /** Runtime diagnostics snapshot (counters / last error / state) */
  fun diagnostics(callback: (Result<Diagnostics>) -> Unit)
  /**
   * Enable or disable hardware-trigger initiated scanning/inventory.
   * When disabled, trigger pulls are ignored and any active inventory is stopped.
   */
  fun setScanningEnabled(enabled: Boolean, callback: (Result<Unit>) -> Unit)

  companion object {
    /** The codec used by FlutterZebraRfid. */
    val codec: MessageCodec<Any?> by lazy {
      FlutterZebraRfidPigeonCodec
    }
    /** Sets up an instance of `FlutterZebraRfid` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: FlutterZebraRfid?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.updateAvailableReaders$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val connectionTypeArg = args[0] as ReaderConnectionType
            api.updateAvailableReaders(connectionTypeArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.startBluetoothScan$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.startBluetoothScan{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.stopBluetoothScan$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.stopBluetoothScan{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.getBondedDevices$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.getBondedDevices{ result: Result<List<BluetoothDevice>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.pairBluetoothDevice$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val addressArg = args[0] as String
            api.pairBluetoothDevice(addressArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.connectReader$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val readerIdArg = args[0].let { num -> if (num is Int) num.toLong() else num as Long }
            api.connectReader(readerIdArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.configureReader$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val configArg = args[0] as ReaderConfig
            val shouldPersistArg = args[1] as Boolean
            api.configureReader(configArg, shouldPersistArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.disconnectReader$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.disconnectReader{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.triggerDeviceStatus$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.triggerDeviceStatus{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.startLocating$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val tagsArg = args[0] as List<RfidTag>
            val disableBeepArg = args[1] as Boolean?
            api.startLocating(tagsArg, disableBeepArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.stopLocating$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.stopLocating{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.resetLocateState$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.resetLocateState{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.currentReader$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.currentReader())
            } catch (exception: Throwable) {
              wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.readerConfig$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.readerConfig{ result: Result<ReaderConfig> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.supportedReaderRegions$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.supportedReaderRegions{ result: Result<List<ReaderRegion>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.setReaderRegion$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val regionCodeArg = args[0] as String
            api.setReaderRegion(regionCodeArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.diagnostics$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.diagnostics{ result: Result<Diagnostics> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfid.setScanningEnabled$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val enabledArg = args[0] as Boolean
            api.setScanningEnabled(enabledArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
/** Generated class from Pigeon that represents Flutter messages that can be called from Kotlin. */
class FlutterZebraRfidCallbacks(private val binaryMessenger: BinaryMessenger, private val messageChannelSuffix: String = "") {
  companion object {
    /** The codec used by FlutterZebraRfidCallbacks. */
    val codec: MessageCodec<Any?> by lazy {
      FlutterZebraRfidPigeonCodec
    }
  }
  fun onAvailableReadersChanged(readersArg: List<Reader>, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfidCallbacks.onAvailableReadersChanged$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(readersArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterRfidError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  fun onReaderConnectionStatusChanged(statusArg: ReaderConnectionStatus, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfidCallbacks.onReaderConnectionStatusChanged$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(statusArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterRfidError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  fun onTagsRead(tagsArg: List<RfidTag>, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfidCallbacks.onTagsRead$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(tagsArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterRfidError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  fun onBatteryDataReceived(batteryDataArg: BatteryData, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfidCallbacks.onBatteryDataReceived$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(batteryDataArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterRfidError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  fun onTagsLocated(tagsArg: List<RfidTag>, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfidCallbacks.onTagsLocated$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(tagsArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterRfidError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  fun onBluetoothDeviceDiscovered(deviceArg: BluetoothDevice, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfidCallbacks.onBluetoothDeviceDiscovered$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(deviceArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterRfidError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  fun onBluetoothScanStatusChanged(statusArg: BluetoothScanStatus, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfidCallbacks.onBluetoothScanStatusChanged$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(statusArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterRfidError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  fun onBluetoothPairingResult(deviceArg: BluetoothDevice, successArg: Boolean, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfidCallbacks.onBluetoothPairingResult$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(deviceArg, successArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterRfidError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  fun onReaderConnectionError(errorArg: ReaderError, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_zebra_rfid.FlutterZebraRfidCallbacks.onReaderConnectionError$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(errorArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterRfidError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
}
