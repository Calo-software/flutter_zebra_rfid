## Agent skills

### Issue tracker

Issues are tracked in ClickUp list `Zebra SDK Flutter Plugin` (`901615175288`) in workspace `36838848`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the five canonical triage labels as ClickUp tags, and map them to the closest list workflow status when updating tasks. See `docs/agents/triage-labels.md`.

### Domain docs

This repo is single-context: one plugin wrapper around the Zebra SDK. See `docs/agents/domain.md`.
